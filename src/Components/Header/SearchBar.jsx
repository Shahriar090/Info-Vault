import React from 'react';

const SearchBar = () => {
  return (
   <input
      type="text"
      placeholder="Search..."
      className="w-full px-4 py-2 rounded-md"
    />
  );
};

export default SearchBar;